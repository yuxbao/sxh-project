## 思享汇 - RAG知识库社区论坛

本项目作为毕业设计。
# sxh-project

本仓库会提交脱敏后的示例配置文件，真实本地配置不再纳入 Git。

本地初始化时请按需复制示例文件，例如：

- `*.env.example -> 对应的 .env 文件`
- `application.example.yml -> 对应的 application.yml`
- `application-dev.example.yml -> 对应的 application-dev.yml`
- `kibana.local.example.yml -> kibana.local.yml`
